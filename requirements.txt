# The ranking engine (rank.py + engine/) uses only the Python standard
# library (json, re, csv, heapq, hashlib, datetime, argparse, gzip) by
# deliberate design — see README.md "Why no ML libraries" for the
# reasoning. There is nothing to install for the engine itself.
#
# Python 3.9+ is required (uses dataclasses-free, but f-string and
# dict-merge features common to 3.9+; tested on 3.11).

# Nothing required. This file exists so `pip install -r requirements.txt`
# is a valid (no-op) step in CI / grading scripts that expect one.
